<Window x:Class="Panaderia.Wpf.Views.ProductosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Productos" Height="500" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Productos"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Tabla -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Productos}"
                  SelectedItem="{Binding ProductoSeleccionado}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" 
                                    Binding="{Binding Nombre}" 
                                    Width="*"/>
                <DataGridTextColumn Header="Rinde" 
                                    Binding="{Binding Rinde}" 
                                    Width="80"/>
                <DataGridTextColumn Header="Costo Total"
                                    Binding="{Binding CostoTotal, StringFormat=\$ {0:N2}}"
                                    Width="120"/>
                <DataGridTextColumn Header="Costo Unitario"
                                    Binding="{Binding CostoUnitario, StringFormat=\$ {0:N2}}"
                                    Width="130"/>
                <DataGridTextColumn Header="Precio Sugerido"
                                    Binding="{Binding PrecioSugerido, StringFormat=\$ {0:N2}}"
                                    Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botones -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Agregar" 
                    Width="90" 
                    Margin="5"
                    Command="{Binding AgregarCommand}"/>
            <Button Content="Editar" 
                    Width="90" 
                    Margin="5"
                    Command="{Binding EditarCommand}"/>
            <Button Content="Ver Receta" 
                    Width="90" 
                    Margin="5"
                    Command="{Binding VerRecetaCommand}"/>
            <Button Content="Eliminar" 
                    Width="90" 
                    Margin="5"
                    Command="{Binding EliminarCommand}"/>
        </StackPanel>
    </Grid>
</Window>
